Thin mauve rule. Use between list rows and above footer bottom bars.

```jsx
<Divider />
<Divider accent />
```

Prefer a background shift (deep → cream) over a divider when separating whole page sections.
