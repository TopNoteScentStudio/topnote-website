Service and partnership cards. Cards never have their own fill — the section background shows through.

```jsx
<Card eyebrow="01" title="Event activations" footer={<Button variant="ghost">Learn more</Button>}>
  One or two sentences.
</Card>
```

`tone="onCream"` swaps the hairline box for a 2px amber top rule and drops the padding box entirely.
