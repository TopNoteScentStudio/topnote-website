Opens every major page section. Titles are ALL CAPS Anybody Bold at the expanded width axis.

```jsx
<SectionHeader eyebrow="What we do" title="Services" subtitle="One line on the offering." />
```

Use `align="center"` for full-bleed CTA bands and intro sections; left for content sections. Set `tone="onCream"` on cream backgrounds.
