Services page body. Alternate `reverse` on every other block so the rhythm zig-zags.

```jsx
<ServiceBlock index="01" title="Event activations" body="…" image="/assets/x.jpg" />
<ServiceBlock index="02" title="Curation" body="…" reverse />
```
