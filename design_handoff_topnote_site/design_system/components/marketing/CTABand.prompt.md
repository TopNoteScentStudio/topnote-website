One per page, near the bottom. Amber tone is the loudest thing in the system — never stack two.

```jsx
<CTABand title="Bring scent to your event" action={<Button variant="secondary">Get in touch</Button>} />
```

On `tone="amber"` the button must be secondary or a deep-filled primary — amber-on-amber is invisible.
